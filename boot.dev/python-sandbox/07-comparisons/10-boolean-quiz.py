# * Boolean quiz
# * Parentheses first, then and, then or — same as JS && / ||.
# ?
# ? is_tall = True
# ? is_bulky = True
# ? is_lean = False
# ? is_short = False
# ? result = (is_tall and is_bulky) or (is_lean and is_short)
# ?
# ? (True and True) or (False and False)
# ? True            or  False
# ? True

# * Q: what is result?
# * A: True
