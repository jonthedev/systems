# * Game Statistics
# * Marketing wants stats about Fantasy Quest game servers:
# * the average number of players across 4 servers.

# * Assignment
# * Debug the code so it correctly prints the solution to the math
# * problem.

# ? The syntax is wrong: it is treating the numbers like plain text.
# ? Strings (plain text) are surrounded in quotes. Integers (numbers)
# ? are not.
# ? "4" is text. 4 is a number. You can average numbers, not quote-wrapped
# ? strings (at least not the way they mean here).
# ? print("(250 + 241 + 244 + 255) / 4")
print((250 + 241 + 244 + 255) / 4)