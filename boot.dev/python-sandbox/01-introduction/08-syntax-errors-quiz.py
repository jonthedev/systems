# * Syntax Errors Quiz
# * Syntax: the rules for how expressions and statements should be
# * structured in a language.

# ? Python (correct):
# ? print("hello world")
# ? Go (correct, different language, different syntax):
# ? fmt.Println("hello world")

# * Syntax errors aren't the only problems you can hit:

# ? Logic bug: the code is valid and runs, but it does something unexpected.
# ? Too slow: the code is valid and correct, but it does it slowly.
# ? This course: syntax errors and logic errors. Performance comes later.
