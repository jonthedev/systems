# * Lesson Failure
# * Boot.dev has two buttons: Run and Submit.

# ? Run: test and debug. No penalty.
# ? Submit: runs your code AND checks it against their answer key.
# ? Fail a submit and you get punished (armor or spree — see below).
# ? Pro tip: you would not deploy to end users without testing.
# ? So do not submit without running first.
# ? Submit is like deploying to production. Run is like trying it locally.

# * What's the Penalty?
# * Fail a lesson and one of two things happens:

# ? If you have armor: you lose one. That armor is consumed to protect
# ? you from future failures on that lesson.
# ? If you have no armor: you lose your sharpshooter spree (resets to 0).
# ? Each time the spree hits 15, you earn a random chest (gems, items
# ? like armor).

# * Assignment
# * Fantasy Quest has a bug in its server code. It should print messages
# * that the game server started successfully.

# ? Fix both syntax errors in the code (same idea as 07: matching quotes,
# ? valid print() calls).
# ? print("Starting up game server...')
# ? prnt("local game server is listening on port 8080")
print("Starting up game server...")
print("local game server is listening on port 8080")

