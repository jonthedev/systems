# * Math
# * We can store and change variables. Now we do math with them.

# ? Addition:       my_sum = a + b
# ? Subtraction:    my_difference = x - y
# ? Multiplication: my_product = c * d
# ? Division:       my_quotient = a / b

# * Order of Operations
# * Parentheses control the order, same as in school (and JS).

# ? first = 5
# ? second = 7
# ? third = 9
# ? average_value = (first + second + third) / 3
# ? print(average_value)  # 7 — average of 5, 7, and 9

# * Assignment
# ? Create armored_health: player_health multiplied by armor_multiplier.

player_health = 1000
armor_multiplier = 2
armored_health = player_health * armor_multiplier

print(armored_health)
