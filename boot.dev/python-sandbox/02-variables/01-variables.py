# * Variables
# * Variables store data as the program runs. Until now we passed
# * values straight into print(). Now we save them so we can reuse
# * and change them before printing.

# * Creating Variables
# * A variable is a name we give to a value.

# ? my_height = 100
# ? my_name = "Lane"
# ? Choose any name, but keep it descriptive and a single token:
# ? continuous text, words separated by underscores (snake_case).
# ? Same idea as a JS let/const name: playerHealth vs player_health.

# * Using Variables
# * Access a value by using the variable's name.

# ? print(my_height)  # prints 100
# ? print(my_name)    # prints Lane

# * Assignment
# * Keep track of our hero's health.

# ? On the first line of real code, create a variable named exactly
# ? player_health and set it equal to 1000.

player_health = 1000
# ? Don't touch below this line
print(player_health)
