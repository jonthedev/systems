# * Whitespace in Python
# * Indent *is* the block. JS uses { }. Python uses 4 spaces (one Tab in
# * Cursor/most editors). Mix tabs and spaces → errors. Skip indent → SyntaxError.
# ?
# ? for i in range(0, 10):
# ?     print(i)          # this line is "in" the loop
# ? print("done")         # this is after the loop (same indent as for)

# * Q: It is ____ to indent the body of a traditional for-loop in Python
# * A: Required
