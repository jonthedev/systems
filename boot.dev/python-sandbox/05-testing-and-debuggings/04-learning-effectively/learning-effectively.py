# * Learning effectively (no assignment code)
# * The course gets harder from here. Process, not talent:
# ?
# ? 1. Read the lesson. Understand the example first.
# ? 2. Read the assignment. Know the goal before typing.
# ? 3. Write a little code.
# ? 4. print() and Run often — small chunks, not a whole file then hope.
# ? 5. Repeat until you trust it.
# ? 6. Submit. Unit-test lessons: leave debug prints. Console lessons: strip them.
# ? 7. After you pass, compare to their solution. That is allowed.
# *
# * Stuck: try Boots for a hint before the answer key.
# * Peeking at the solution every time is how you fake-finish the chapter.
# * Reset (↶) if you lost track of their starter vs your edits.
# *
# * This matches what we already do locally: type it, run python3, then
# * comments. Instructor solution is for after you pass, not instead of.
