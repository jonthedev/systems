# Testing and debugging (Boot.dev ch. 5)

Your solutions and notes live here. **Boot.dev’s `main_test.py` files are not copied.**

Those runners are their grader (call your function, check the return). The site already runs them. Copying them locally meant `from main import …`, hyphenated filenames, and `__pycache__` for no extra learning — we are not writing pytest yet.

- Pass/fail: Boot.dev Run / Submit.
- Local: retype your function, `python3` it if you want a print, comments as usual.
- Real unit tests later: [pytest](https://docs.pytest.org/en/stable/).
