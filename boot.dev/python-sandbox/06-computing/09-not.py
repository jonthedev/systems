# * not — flips True ↔ False
# * The third logical operator (with and / or).
# ?
# ? not True   →  False
# ? not False  →  True
# ?
# ? JS: ! is not
# ?     !true   →  false
# ?     !false  →  true
# ?
# ? Ops flavour: "restart if *not* healthy"
# ?     if not is_healthy: restart()

# * Q: The operators and, or, and not are ____ operators.
# * A: logical
