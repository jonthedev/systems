# * Numbers review (quiz)
# * Two numerical types in Python: integers and floats.
# ?
# ? my_int = 5      # int — whole number (also -3)
# ? my_float = 5.5  # float — has a decimal
# ?
# ? JS: both are usually just Number. Python keeps them as separate types.
# ? typeof 5 === "number" && typeof 5.5 === "number"
# ? type(5) is int, type(5.5) is float

# * Q: ____ and ____ are numerical types in Python.
# * A: integers and floats
