# Command line

## Variables

```bash
name=Gandalf              # no spaces around =
echo $name
echo "Hello, $name"
echo '$name'              # literal $name — single quotes don't expand
```

Session only. Close the terminal, it's gone.

## History

```bash
history
```

Numbered list of past commands. Secrets typed here stay in the list.
